# Copy to `.env` (git-ignored) or `export` these in your shell.
# Keys are read by name via `api_key_env` in your agent config — never hard-code keys.

# Aliyun Bailian (百炼) / DashScope — for --client dashscope / configs/bailian.toml
DASHSCOPE_API_KEY=sk-...

# Anthropic (Claude) — for --client anthropic
# ANTHROPIC_API_KEY=sk-ant-...
